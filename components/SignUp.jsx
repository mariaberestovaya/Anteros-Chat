import { Button, Divider, Form, Grid, Segment } from "semantic-ui-react";

const SignUp = (props) => {
  return (
    <Grid.Column verticalAlign="middle">
      <Button content="Sign up" icon="signup" size="big" disabled />
    </Grid.Column>
  );
};

export default SignUp;
